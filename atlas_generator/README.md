# atlas_generator

Asset pipeline tool to generate packed buffers of display data.

Extracts data from scenes (SVG) and fonts (TTF, OTF), and generates buffers of data tailored for CPU
and GPU consumption.
