[package]
name = "trade_client"
version = "0.1.0"
edition = "2024"
description = "Client for querying trade server information and issuing commands"

[dependencies]
cast = { path = "../cast" }
error = { path = "../error" }
