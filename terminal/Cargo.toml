[package]
name = "terminal"
version = "0.1.0"
edition = "2024"
"description" = "Terminal host for CLI processes"

[dependencies]
buffer = { path = "../buffer" }
cast = { path = "../cast" }
error = { path = "../error" }
