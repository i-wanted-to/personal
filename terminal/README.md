# terminal

Terminal host for CLI processes. Captures live input/output from a command-line process and exposes
it for easy processing in a terminal-independent manner, including deocding and filtering of control
codes.
