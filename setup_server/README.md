# setup_server

Initialize a remote computer with users and networking rules.

Interacts with linux-style target over SSH to prepare it to run applications deployed by the
`deploy` utility without any manual configuration.
