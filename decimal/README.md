# decimal

Number type that precisely represents fractional base-10 values.

Supports conversion to and from floating point, conversion to and from strings, and compile time
computations. Only checked arithemtic operations are exposed, guaranteeing no overflow or loss of
precision.
