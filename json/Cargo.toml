[package]
name = "json"
version = "0.1.0"
edition = "2024"
description = "JSON object manipulation with parsing and serialization"

[dependencies]
cast = { path = "../cast" }
error = { path = "../error" }
