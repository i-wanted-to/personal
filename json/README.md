# json

JSON object manipulation with parsing and serialization. Can operate in a hierarchy mode, where the
entire document is parsed into a tree of structures, and an iterative mode where individual values
are read and written without extra allocations.