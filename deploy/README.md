# deploy

Deploys a hosted application, synchronizing files and registering services.

Source is expected to be a directory hierarchy containing a standalone, argumentless executable in
the root. Destination is expected to be a SSH connection target with sufficient privileges to manage
users and services.
