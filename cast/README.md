# cast

Utilities for exact type conversions.

- Conversion between integer and floating point that fails if the conversion is not exact.
- Compile-time conversion between all basic integer types with bounds checks to ensure the
  conversion is exact.
