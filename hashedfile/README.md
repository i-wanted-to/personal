# hashedfile

Manage a directory of hashed files and references. Exposes functionality to:
- Add files to the directory
- Select files by combinations of name, type, and hash
- Populate templates, replacing file references with the corresponding hashed
  file name
