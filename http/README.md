# http

HTTP server and client with websocket support. Supports both unencrypted (HTTP) and encrypted
(HTTPS) connections. Functionality exposed via an event-based interface guaranteed to never block.
