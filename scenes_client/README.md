# scenes_client

Demonstration of the basic app platform functionality (data fetching, vector rendering, audio
output, positional input, and animation).

Lets users explore a sequence of scenes. Each scene is a vector image (SVG), with named elements
animating, responding to user input, and playing audio.
